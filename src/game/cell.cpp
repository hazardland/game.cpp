#include "game/cell.h"


// #include <game/state.h>
#include "game/object.h"
#include "game/terrain.h"

Cell::Cell(int layers) : objects(layers) {
    // The 'objects' vector is initialized with 'layers' number of empty lists.
}
